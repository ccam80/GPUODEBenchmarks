# Timing sweep over trajectory counts: every GPU package, every problem it implements, every algorithm it runs, at one fixed step and one tolerance.

[set]
packages   = ["cubie", "cubie_mlir", "jax", "pytorch", "myokit_cuda", "cpp", "julia_gpu"]
problems   = "all"
algorithms = "all"
precision  = "float32"
finals     = false
transfers  = ["both", "none"]
build      = "warm"

[[grid]]
packages = "all"
parameter = "default"
scale = "default"
min = "default"
max = "default"
n = [8, 32, 128, 512, 2048, 8192, 32768, 131072, 524288, 2097152, 8388608, 16777216]
system_params = {}

[[stepping]]
packages = "all"
algorithms = "all"
controller = "fixed"
dt = {duration_times_2_pow = [-10]}
newton = {atol = 1.0e-6, rtol = 1.0e-6}

[[stepping]]
packages = "all"
algorithms = "all"
controller = "default"
tol = [1.0e-5]
dt0 = {duration_times_2_pow = -10}
dt_min = {duration_times = 1.0e-6}
dt_max = "none"
newton = "tol"
gains = {}

[[stepping]]
packages = ["cubie", "cubie_mlir"]
algorithms = "all"
controller = "pi"
tol = [1.0e-5]
dt0 = {duration_times_2_pow = -10}
dt_min = {duration_times = 1.0e-6}
dt_max = "none"
newton = "tol"
gains = "dirk_defaults"
